<launch>
  <arg name = "ns" default="$(optenv RMTT_NAMESPACE /)" />
  <arg name="launch_prefix" default="" /> <!-- set to value="gdbserver localhost:10000" for remote debugging -->
  <arg name="camera_name" default="$(arg ns)"/>
  <arg name="camera_frame" default="camera_link" />
  <arg name="image_topic" default="image_raw" />

  <!-- Set parameters -->

  <group ns="$(arg ns)">
    <param name="apriltag_ros/camera_frame" type="str" value="$(arg ns)/$(arg camera_frame)" />
    <node pkg="apriltag_ros" type="apriltag_ros_continuous_node" name="apriltag_ros" clear_params="true" output="screen" launch-prefix="$(arg launch_prefix)" >
      <!-- Remap topics from those used in code to those on the ROS network -->
      <remap from="image_rect" to="$(arg image_topic)" />
      <remap from="camera_info" to="camera_info" />
      <rosparam command="load" file="$(find rmtt_apriltag)/config/settings.yaml" />
      <rosparam command="load" file="$(find rmtt_apriltag)/config/tags.yaml" />
      <param if="$(eval ns=='/')" name="camera_frame" type="str" value="$(arg camera_frame)" />
      <param name="publish_tag_detections_image" type="bool" value="false" />      <!-- default: false -->
    </node>
  </group>
</launch>
